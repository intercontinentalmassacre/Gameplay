<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Pluvia" parent="android:Theme.Material.NoActionBar">
        <!-- Android 12+ otherwise injects a differently coloured default splash. -->
        <item name="android:windowBackground">@color/window_background_color</item>
        <item name="android:colorBackground">@color/window_background_color</item>
        <item name="android:windowSplashScreenBackground">@color/window_background_color</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
